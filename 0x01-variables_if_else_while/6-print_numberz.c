#include <stdio.h>

/**
 * Description: main-program entry point.
 * Return:0 no error, non zero if error.
 */

int main(void)
{
	int n;

	for (n = "0"; n <= "9"; n++)
		putchar(n);
	putchar("\n");
	return (0);
}
