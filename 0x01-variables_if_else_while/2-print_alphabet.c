#include <stdio.h>
#include <ctype.h>

/**
 * Description: main - program that prints the alphabet in lowercase.
 * You can only use the putchar function.
 * Return: 0
 */

int main(void)
{
int x = 'a';
while (x <= 'z')
{
putchar(l);
x += 1;
}
putchar('\n');
return (0);
}
